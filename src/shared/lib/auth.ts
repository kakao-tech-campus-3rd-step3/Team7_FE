// 현재 로그인한 사용자의 memberId 반환
// TODO: 추후 JWT 토큰에서 실제 memberId를 가져오도록 수정하기
// return : 현재 사용자 memberId

export function getCurrentMemberId(): number {
    return 1;
}

// 현재 로그인한 멘토의 mentorId 반환
// TODO: 추후 JWT 토큰에서 실제 mentorId를 가져오도록 수정하기
// return : 현재 멘토 mentorId

export function getCurrentMentorId(): number {
    return 1;
}
